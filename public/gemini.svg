      function sendMessage() {
        var userInput = document.getElementById("userInput").value;
        displayMessage("user", userInput);
        document.getElementById("userInput").value = "";

        // Send message to server and display response
        var xhr = new XMLHttpRequest();
        xhr.open("POST", "/api/sendmessage", true);
        xhr.setRequestHeader("Content-Type", "application/json");
        xhr.onreadystatechange = function () {
          if (xhr.readyState === 4 && xhr.status === 200) {
            var response = JSON.parse(xhr.responseText);
            displayMessage("bot", response.message);
          }
        };
        xhr.send(JSON.stringify({ message: userInput }));
      }

      function displayMessage(sender, message) {
        var chatBox = document.getElementById("chatBox");
        var messageElement = document.createElement("div");
        messageElement.innerHTML = "<span style='font-weight: bold'>" + (sender === "user" ? "You:" : "Bot:") + "</span> " + message;
        chatBox.appendChild(messageElement);
        chatBox.scrollTop = chatBox.scrollHeight;
      }
      function sendMessage() {
        var userInput = document.getElementById("userInput").value;
        displayMessage("user", userInput);
        document.getElementById("userInput").value = "";

        // Send message to server and display response
        var xhr = new XMLHttpRequest();
        xhr.open("POST", "/api/sendmessage", true);
        xhr.setRequestHeader("Content-Type", "application/json");
        xhr.onreadystatechange = function () {
          if (xhr.readyState === 4 && xhr.status === 200) {
            var response = JSON.parse(xhr.responseText);
            displayMessage("bot", response.message);
          }
        };
        xhr.send(JSON.stringify({ message: userInput }));
      }

      function displayMessage(sender, message) {
        var chatBox = document.getElementById("chatBox");
        var messageElement = document.createElement("div");
        messageElement.innerHTML = "<span style='font-weight: bold'>" + (sender === "user" ? "You:" : "Bot:") + "</span> " + message;
        chatBox.appendChild(messageElement);
        chatBox.scrollTop = chatBox.scrollHeight;
      }
        var xhr = new XMLHttpRequest();
        xhr.open("POST", "/api/sendmessage", true);
        xhr.setRequestHeader("Content-Type", "application/json");
        xhr.onreadystatechange = function () {
          if (xhr.readyState === 4 && xhr.status === 200) {
            var response = JSON.parse(xhr.responseText);
            displayMessage("bot", response.message);
          }
        };
        xhr.send(JSON.stringify({ message: userInput }));

        var xhr = new XMLHttpRequest();
        xhr.open("POST", "/api/sendmessage", true);
        xhr.setRequestHeader("Content-Type", "application/json");
        xhr.onreadystatechange = function () {
          if (xhr.readyState === 4 && xhr.status === 200) {
            var response = JSON.parse(xhr.responseText);
            displayMessage("bot", response.message);
          }
        };
        xhr.send(JSON.stringify({ message: userInput }));

        var userInput = document.getElementById("userInput").value;
        displayMessage("user", userInput);
        document.getElementById("userInput").value = "";

        // Send message to server and display response
        var xhr = new XMLHttpRequest();
        xhr.open("POST", "/api/sendmessage", true);
        xhr.setRequestHeader("Content-Type", "application/json");
        xhr.onreadystatechange = function () {
          if (xhr.readyState === 4 && xhr.status === 200) {
            var response = JSON.parse(xhr.responseText);
            displayMessage("bot", response.message);
          }
        };
        xhr.send(JSON.stringify({ message: userInput }));
<svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
<foreignObject width="100%" height="100%">
  <body xmlns="http://www.
myw3.org/1999/xhtml">
    <div style="display: flex; height: 100%; width: 100%; align-items: center; justify-content: center">
      <div style="display: flex; flex-direction: column; align-items: center">
        <span style="font-size: 14px; margin-bottom: 10px;">
          Q-DEF Assist
        </span>
        <input type="text" id="userInput" placeholder="Ask a question..." style="padding: 8px; border: 1px solid #ccc; border-radius: 4px; margin-bottom: 10px;">
        <button onclick="sendMessage()" style="padding: 8px 12px; background-color: #4CAF50; color: white; border: none; border-radius: 4px; cursor: pointer;">Send</button>
        <div id="chatBox" style="width: 100%; height: 100%; overflow-y: auto; margin-top: 10px">
          </div>
      </div>
    </div>
    <script>
      function sendMessage() {
        var userInput = document.getElementById("userInput").value;
        // Here you would implement your logic to send the user's input to your conversational agent.
        // For now, let's just display the user's input in the chat box
        displayMessage("user", userInput);
        document.getElementById("userInput").value = "";
      }

      function displayMessage(sender, message) {
        var chatBox = document.getElementById("chatBox");
        var messageElement = document.createElement("div");
        messageElement.innerHTML = "<span style='font-weight: bold'>" + (sender === "user" ? "You:" : "Bot:") + "</span> " + message;
        chatBox.appendChild(messageElement);
        chatBox.scrollTop = chatBox.scrollHeight;
      }

    </script>
  </body>
</foreignObject>
</svg>